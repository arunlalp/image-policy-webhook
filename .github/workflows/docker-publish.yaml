name: Docker build pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: [self-hosted, image-builder]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: sudo docker build -t techiescamp/go-color-app:latest ./image-policy-webhook

      - name: Push Docker image
        run: sudo docker push techiescamp/go-color-app:latest
